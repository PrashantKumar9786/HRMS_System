services:
  - type: web
    name: hrms-backend
    env: node
    buildCommand: npm install && npx prisma generate && npx prisma db push
    startCommand: node src/app.js
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: PORT
        value: 5000
